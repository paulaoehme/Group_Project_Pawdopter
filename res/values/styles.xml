<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light"></style>

    <!-- Customize your theme here. -->
    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/toolbar</item>
        <item name="android:titleTextColor">@color/title_color</item>
    </style>

    <!-- Menu item theme. -->
    <style name="CustomPopupStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/colorAccent</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-5dp</item>
        <item name="android:textColor">@color/title_color</item>
    </style>
</resources>
